export class Tarefa{

    constructor(
        public id?: number,
        public nome?: string,
        public concluida?: boolean){}
    // ? significa que seja opcional, criar valores sem se for obrigatório
}